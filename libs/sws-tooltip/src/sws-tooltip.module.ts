import { NgModule } from '@angular/core';
import { SWSTooltipDirective } from './sws-tooltip.directive';

@NgModule({
  imports: [],
  exports: [SWSTooltipDirective],
  declarations: [SWSTooltipDirective]
})
export class SWSTooltipModule { }